# Stepfunctor Execution Package

This is the infrastructure package `stepfunctor-exec` for `stepfunctor`. It serves
two purposes:

- help build the code bundle for lambdas used by `stepfunctor-infra`.
- execute step functions locally for testing purposes

For more information see the [github page](https://github.com/torfmaster/stepfunctor).
