# stepfunctor-exec

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build stepfunctor-exec` to build the library.

## Running unit tests

Run `nx test stepfunctor-exec` to execute the unit tests via [Jest](https://jestjs.io).
