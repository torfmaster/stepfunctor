# stepfunctor-lang

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build stepfunctor-lang` to build the library.

## Running unit tests

Run `nx test stepfunctor-lang` to execute the unit tests via [Jest](https://jestjs.io).
